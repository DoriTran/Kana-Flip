import { Download, Settings, Trash2, Upload, X } from 'lucide-react'
import { useEffect, useRef, useState } from 'react'
import { useKanaStore } from '../stores/kanaStore'

export function SettingsDialog({onClose}:{onClose:()=>void}){const {preferences,setPreferences,clearAll}=useKanaStore();const dialog=useRef<HTMLDivElement>(null);const [confirming,setConfirming]=useState(false);useEffect(()=>{dialog.current?.focus();const key=(e:KeyboardEvent)=>e.key==='Escape'&&onClose();document.addEventListener('keydown',key);return()=>document.removeEventListener('keydown',key)},[onClose]);return <div className="modal-backdrop" role="presentation" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><div className="dialog" role="dialog" aria-modal="true" aria-labelledby="settings-title" tabIndex={-1} ref={dialog}><div className="dialog-title"><h2 id="settings-title"><Settings/> Settings & Data</h2><button className="icon-button" onClick={onClose} aria-label="Close"><X/></button></div><section><h3>Appearance</h3><label className="switch-row">Show keyboard hints <input type="checkbox" checked={preferences.showKeyboardHints} onChange={e=>setPreferences({showKeyboardHints:e.target.checked})}/></label><label className="switch-row">Reduced motion <input type="checkbox" checked={preferences.reducedMotion} onChange={e=>setPreferences({reducedMotion:e.target.checked})}/></label></section><section><h3>Study</h3><label className="switch-row">Confirm before discarding study <input type="checkbox" checked={preferences.confirmDiscard} onChange={e=>setPreferences({confirmDiscard:e.target.checked})}/></label></section><section><h3>Data</h3><button disabled><Download/> Export progress <small>Sắp có</small></button><button disabled><Upload/> Import progress <small>Sắp có</small></button>{confirming?<div className="confirm-box"><p>Delete all progress, history and settings?</p><div><button onClick={()=>setConfirming(false)}>Cancel</button><button className="danger" onClick={()=>{clearAll();onClose()}}>Yes, clear data</button></div></div>:<button className="danger ghost" onClick={()=>setConfirming(true)}><Trash2/> Clear all data</button>}</section><p className="fine-print">Clearing data is permanent and cannot be undone.</p></div></div>}
